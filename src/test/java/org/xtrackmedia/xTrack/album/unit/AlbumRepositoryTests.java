package org.xtrackmedia.xTrack.album.unit;

class AlbumRepositoryTests {

//    @Mock
//    private AlbumRepository albumRepository;
//
//    @InjectMocks
//    private AlbumService albumService;
//
//    @Test
//    void testFindByTitle() {
//        // Arrange
//        String title = "Phantom Liberty";
//        List<Album> expectedAlbums = Collections.singletonList(new Album(1L, title, "Dawid Podsiadlo / P.T. Adamczyk", 2023, "Pop"));
//        when(albumRepository.findByTitle(title)).thenReturn(expectedAlbums);
//
//        // Act
//        List<Album> actualAlbums = albumRepository.findByTitle(title);
//
//        // Assert
//        assertEquals(expectedAlbums, actualAlbums);
//        verify(albumRepository, times(1)).findByTitle(title);
//    }
//
//    @Test
//    void testFindByTitleAndArtist() {
//        // Arrange
//        String title = "Phantom Liberty";
//        String artist = "Dawid Podsiadlo / P.T. Adamczyk";
//        List<Album> expectedAlbums = Collections.singletonList(new Album(1L, title, artist, 2023, "Pop"));
//        when(albumRepository.findByTitleAndArtist(title, artist)).thenReturn(expectedAlbums);
//
//        // Act
//        List<Album> actualAlbums = albumRepository.findByTitleAndArtist(title, artist);
//
//        // Assert
//        assertEquals(expectedAlbums, actualAlbums);
//        verify(albumRepository, times(1)).findByTitleAndArtist(title, artist);
//    }
}
